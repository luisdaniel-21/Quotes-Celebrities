<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:usesCleartextTraffic="true"
        android:name=".QuotesApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/itvo_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/itvo_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quotescelebrities"
        tools:targetApi="31">
        <activity
            android:name=".presentation.view.DrawerNavigation"
            android:exported="true"
            android:label="@string/title_activity_drawer_navigation"
            android:theme="@style/Theme.Quotescelebrities.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

    </application>

</manifest>